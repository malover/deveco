# Configuration for document-project2 skill.
# Edit this file to customize the skill's behavior for your project.

# ── Runtime Configuration ──
[config]
# Where generated documentation is written (relative to project root).
project_knowledge = "{project-root}/docs"

# Language for agent messages during the workflow.
communication_language = "English"

# Language for all generated document content.
document_output_language = "English"

# Your name (used for greetings). Leave empty to be prompted.
user_name = ""

# ── Workflow Customization ──
[workflow]

# Steps to run before the standard activation (config load, greet).
activation_steps_prepend = []

# Steps to run after greet but before the workflow begins.
activation_steps_append = []

# Persistent facts the workflow keeps in mind for the whole run
# (standards, compliance constraints, stylistic guardrails).
# Each entry is either:
#   - a literal sentence, e.g. "All functions must have JSDoc comments."
#   - a file reference prefixed with `file:`, e.g. "file:{project-root}/docs/standards.md"
#     (glob patterns are supported; the file's contents are loaded and treated as facts).
persistent_facts = [
  "file:{project-root}/**/project-context.md",
]

# Executed when the workflow reaches its terminal stage, after
# the main output has been delivered. Leave empty for no post-completion behavior.
on_complete = """After documentation is generated, report whether the HomeGraph index and generated diagrams may be stale:

1. HOMEGRAPH REFRESH: If HomeGraph was used and source files changed during or after the scan, recommend refreshing/syncing the HomeGraph index before the next documentation run. Do not switch to a CodeToGraph provider automatically.

2. DIAGRAMS PRESERVATION: The docs/diagrams/ directory may contain .mmd diagrams synthesized from HomeGraph caller/callee/explore results plus verified source reads. INDEX.md catalogs them. Keep these diagrams in sync with architecture.md when documentation is regenerated.

If HomeGraph is unavailable, report the missing graph capability but do not block completion — documentation may still be generated with direct file scanning."""
